import TipTapDropcursor from '@tiptap/extension-dropcursor';

const Dropcursor = TipTapDropcursor.configure({
  width: 2,
  class: 'ProseMirror-dropcursor border-black',
});
export default Dropcursor;
